{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions granted to the main wallet window. Tauri 2.x denies every plugin command unless it's listed here, so each plugin we use needs its `:default` set explicitly.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default",
    "shell:default",
    "updater:default"
  ]
}
