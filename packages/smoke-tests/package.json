{
  "name": "@smirk/smoke-tests",
  "version": "0.3.0",
  "private": true,
  "description": "Two-wallet automated smoke harness driving @smirk/core API client end-to-end against production. Mainnet, real (small) funds, cron-deployable to the tipbot server. See STRUCTURAL_DEBT.md \"wallet-to-wallet integration tests\" for the design rationale.",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx 'src/**/__tests__/*.test.ts'",
    "smoke": "tsx src/main.ts",
    "smoke:dev": "tsx src/main.ts",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@smirk/assets": "*",
    "@smirk/core": "*"
  }
}
