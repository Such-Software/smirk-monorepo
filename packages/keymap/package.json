{
  "name": "@smirk/keymap",
  "version": "0.3.0",
  "private": true,
  "description": "Cross-platform keyboard-shortcut registry. Defines the canonical action set (focus-url-bar, new-tab, lock-wallet, etc.) and the per-platform bindings (Mac Cmd, Win/Linux Ctrl, mobile gestures). Consumers bind via useKeymap() rather than addEventListener, so shortcuts stay consistent across extension/desktop/mobile.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx src/__tests__/keymap.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
