{
  "name": "@smirk/assets",
  "version": "0.3.1",
  "description": "Asset registry — pure-data definitions of every chain Smirk supports. Decimals, family classification, capabilities, confirmation requirements. Imports nothing chain-specific.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist/",
    "src/"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --import tsx 'src/**/*.test.ts'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "tsx": "^4.7.0"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
